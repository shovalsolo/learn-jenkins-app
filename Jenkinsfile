pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                sh '''
                    echo "Hello without "
                    pwd
                    ls -la
                '''
            }
        }
    }
}
